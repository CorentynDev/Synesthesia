<?xml version="1.0" encoding="utf-8"?>
<manifest xmlns:android="http://schemas.android.com/apk/res/android"
    xmlns:tools="http://schemas.android.com/tools">
    <uses-permission android:name="android.permission.INTERNET"/>

    <application
        android:allowBackup="true"
        android:dataExtractionRules="@xml/data_extraction_rules"
        android:fullBackupContent="@xml/backup_rules"
        android:icon="@mipmap/ic_launcher"
        android:label="@string/app_name"
        android:roundIcon="@mipmap/ic_launcher_round"
        android:supportsRtl="true"
        android:theme="@style/Theme.Synesthesia"
        tools:targetApi="31">

        <!-- LoginActivity devient l'activité de lancement -->
        <activity
            android:name=".LoginActivity"
            android:exported="true">
            <intent-filter>
                <action android:name="android.intent.action.MAIN" />
                <category android:name="android.intent.category.LAUNCHER" />
            </intent-filter>
        </activity>

        <!-- RegisterActivity (Activité pour créer un compte) -->
        <activity android:name=".RegisterActivity" android:exported="true"/>

        <!-- MainActivity -->
        <activity android:name=".MainActivity" android:exported="true" />

        <!-- CreateRecommendationActivity (permet de créer des recommandations) -->
        <activity android:name=".CreateRecommendationActivity" />

        <!-- SearchBookActivity -->
        <activity android:name=".SearchBookActivity" />
    </application>

</manifest>
